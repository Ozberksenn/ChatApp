import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    marginHorizontal: 10,
  },
  image: {
    width: 60,
    height: 60,
    borderRadius: 60,
    borderWidth: 1,
    borderColor: "#red",
  },
  name: {
    fontSize: 16,
    color: "white",
    textAlign: "center",
  },
});
